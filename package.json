{
	"name": "pokemon-start-app",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "next dev",
		"build": "prisma generate && next build",
		"start": "next start",
		"lint": "next lint",
		"postinstall": "prisma generate",
		"stripewebhook": "stripe listen --forward-to localhost:420/api/stripe/webhook",
		"webhook": "stripe listen --forward-to http://localhost:420/api/merchandise/webhooks/stripe/transact",
		"eventtrigger": "stripe trigger payment_intent.succeeded",
		"postgresqlinstall": "bunx prisma init --datasource-provider postgresql",
		"setdb": "prisma db pull",
		"vercel-build": "prisma generate && prisma migrate deploy && next build",
		"create-migrate": "prisma migrate dev --name init",
		"migrate": "prisma migrate deploy",
		"view-db": "prisma studio"
	},
	"dependencies": {
		"@prisma/client": "^5.20.0",
		"axios": "^1.7.7",
		"framer-motion": "^11.11.8",
		"next": "14.2.15",
		"openai": "^4.67.3",
		"react": "^18",
		"react-confetti": "^6.1.0",
		"react-dom": "^18",
		"react-icons": "^5.3.0"
	},
	"devDependencies": {
		"@types/node": "^20",
		"@types/react": "^18",
		"@types/react-dom": "^18",
		"eslint": "^8",
		"eslint-config-next": "14.2.15",
		"postcss": "^8",
		"prisma": "^5.20.0",
		"tailwindcss": "^3.4.1",
		"typescript": "^5"
	}
}
